# 📦 Інструкція з резервного копіювання SmartInvent_1

Цей документ описує стратегію резервного копіювання для проєкту SmartInvent_1. Резервне копіювання є критичним для збереження даних та відновлення після збоїв.

---

## 🛡️ Стратегія резервного копіювання

- **Повне резервне копіювання**: Зберігаються всі дані проєкту, включаючи бази даних, файли конфігурацій, логі та користувацькі дані.
- **Інкрементальне резервне копіювання**: Зберігаються тільки зміни, що відбулися з останнього повного або інкрементального резервного копіювання.
- **Диференціальне резервне копіювання**: Зберігаються зміни, що відбулись з останнього повного резервного копіювання.

---

## 🔄 Типи резервних копій

- **Повне резервне копіювання**:
    - Кожен раз копіюються всі дані.
    - Використовується для створення стартової резервної копії або після значних змін.
- **Інкрементальне резервне копіювання**:
    - Створюється кожен раз після повного або попереднього інкрементального копіювання.
    - Зберігаються тільки зміни, що відбулись після останнього резервного копіювання.
- **Диференціальне резервне копіювання**:
    - Зберігаються зміни, що відбулись після останнього повного резервного копіювання.

---

## ⏲️ Частота створення резервних копій

- **Бази даних**: Резервне копіювання бази даних повинно відбуватись не рідше ніж один раз на день.
- **Конфігураційні файли**: Резервне копіювання файлів конфігурацій повинно виконуватись після кожного оновлення.
- **Користувацькі дані**: Резервне копіювання даних повинно виконуватись залежно від змісту користувацьких даних (наприклад, раз на тиждень).
- **Логи системи**: Резервне копіювання логів слід робити при кожному завершенні роботи або щонайменше раз на день.

---

## 💾 Зберігання та ротація копій

1. **Зберігання копій**:
    - Використовуйте сховище на зовнішньому сервері або в хмарному середовищі (наприклад, AWS S3).
    - Копії мають бути збережені в кількох фізичних місцях для забезпечення надійності.

2. **Ротація копій**:
    - Після певної кількості створених резервних копій (наприклад, 7) найстаріші копії повинні бути видалені, щоб забезпечити достатньо місця для нових.

---

## 📋 Процедура резервного копіювання

1. **Резервне копіювання бази даних**:
    - Використовуйте команду `pg_dump` для створення резервної копії бази даних:
      ```bash
      pg_dump -U smart_user -h localhost -F c smartinvent_prod > /path/to/backups/smartinvent_prod_$(date +%F).sql
      ```

2. **Резервне копіювання файлів конфігурацій**:
    - Використовуйте команду `cp` для копіювання конфігурацій:
      ```bash
      cp /path/to/SmartInvent_1/src/main/resources/application.properties /path/to/backups/application.properties_$(date +%F)
      ```

3. **Резервне копіювання користувацьких даних**:
    - Створюйте архіви з користувацькими даними, використовуючи `tar` або `rsync`:
      ```bash
      tar -cvf /path/to/backups/user_data_$(date +%F).tar.gz /path/to/user/data/
      ```

4. **Резервне копіювання логів системи**:
    - Копіюйте важливі логи:
      ```bash
      cp /var/log/smartinvent/*.log /path/to/backups/logs/
      ```

---

## 🛠️ Автоматизація процесу резервного копіювання

Для автоматизації можна використовувати `cron` (для Linux) або планувальник завдань у Windows.

1. **Автоматизація з cron**:
    - Для щоденного резервного копіювання можна додати запис у cron:
      ```bash
      0 2 * * * /path/to/backup.sh
      ```

2. **Автоматизація за допомогою скриптів**:
    - Створіть скрипт, що автоматизує всі ці процеси резервного копіювання.
    - Наприклад, створіть файл `backup.sh`, який буде запускати всі вищеописані команди.

---

## 🔄 Процедура відновлення з резервних копій

1. **Повне відновлення системи**:
    - Відновіть резервні копії бази даних:
      ```bash
      pg_restore -U smart_user -h localhost -d smartinvent_prod /path/to/backups/smartinvent_prod_YYYY-MM-DD.sql
      ```

    - Відновіть файли конфігурацій:
      ```bash
      cp /path/to/backups/application.properties_YYYY-MM-DD /path/to/SmartInvent_1/src/main/resources/application.properties
      ```

    - Відновіть користувацькі дані:
      ```bash
      tar -xvf /path/to/backups/user_data_YYYY-MM-DD.tar.gz -C /path/to/user/data/
      ```

    - Перезавантажте сервер:
      ```bash
      sudo systemctl restart smartinvent
      ```

2. **Вибіркове відновлення даних**:
    - Відновіть окремі файли або таблиці з резервних копій за допомогою інструментів, таких як `pg_restore`.

---

## 🧪 Тестування відновлення

- Перевірте, що всі дані були відновлені належним чином.
- Перевірте працездатність сервісу:
  ```bash
  systemctl status smartinvent
